{
  "name": "url-short-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root package to delegate commands to frontend/",
  "scripts": {
    "start": "npm --prefix frontend start",
    "build": "npm --prefix frontend build",
    "test": "npm --prefix frontend test"
  }
}
